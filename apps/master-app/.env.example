# HTTP API for tactical sync polling (master desktop — direct URL, not Discord proxy).
VITE_AMBER_SYNC_API_BASE_URL=https://api.ambercoffer.belinde.click

# Dev environment:
# VITE_AMBER_SYNC_API_BASE_URL=https://api-dev.ambercoffer.belinde.click
